# Run with `dart run ffigen --config ffigen.yaml`.
name: Markdown2htmlBindings
description: |
  Bindings for `src/markdown2html.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/markdown2html_bindings_generated.dart'
headers:
  entry-points:
    - 'src/markdown2html.h'
  include-directives:
    - 'src/markdown2html.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
